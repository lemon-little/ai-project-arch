# Source Version

`source.json` pins the exact VERL revision used to author and verify this atlas, plus the Archify generator revision and local tracked-diff digest used to render it.

The VERL tracked source tree matched `HEAD`. Its reported dirty state comes from the 88 untracked generated files under `docs/archify/` and `docs/verl-runtime-architecture*`; these files are archived here. Every source link embedded in the diagrams is pinned to commit `bd6f5645023a774e75b3ea1ed63f652feb922ace`.
